fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
alembic>=1.14
pydantic-settings>=2.7
pillow>=11
pyyaml>=6
httpx>=0.28
# 测试依赖（pytest）由 pyproject.toml dev extra 管理：pip install -e .[dev]
# 生产 PostgreSQL 需另装驱动：pip install "psycopg[binary]>=3"

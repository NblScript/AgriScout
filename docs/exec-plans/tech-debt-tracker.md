# 技术债与可选打磨追踪

> 已知未修项的台账。新增债务追加一行，修掉后移到「已清」并注 commit。
> 判断标准：知道它存在、暂时不修、但演示/答辩/扩展前必须有人记得的东西。

## 未清

| 债 | 影响 | 触发条件（什么时候必须还） | 备注 |
|---|---|---|---|
| 无用户认证 | 接口全开放，多租户/边界问题无话术 | 被评委追问、或部署到公网 | B4 有插槽，加 JWT 零改接口 |
| 几何裸浮点 | 无空间索引，大数据量 bbox 查询变慢 | 数据量 >10 万点 | B1 预留 PostGIS 迁移路径 |
| 标注 bbox 画框 | 标注只有整图级标签，YOLO 病害框训练缺框 | 启动病害训练时 | 表已预留 bbox JSON 字段 |
| YOLO 导出 ONNX | 后端推理依赖完整 torch（~2GB） | 后端部署到小内存机器时 | ultralytics 支持 onnxruntime 推理 |
| 胁迫色公式不分生育期 | 成熟期麦田整体偏黄→stress_ratio 高→risk 误判满分（真实照片验证暴露） | 真车数据进来后 | risk 公式按生育期条件化（拔节期绿、灌浆期黄是常态） |
| 规则农业可信度 | wheat.yaml 阈值「待农技复核」 | 答辩前 | 找农学院/文献补出处 |
| pnpm/chrome 依赖文档化 | 前端 dev 的 XDG_* 环境变量只在 README，忘记就起不来 | 下次环境重建 | 可写进 package.json scripts |
| 离线瓦片仅覆盖演示区域 | 拖出 2.5km 缓存圈或缩放超 13-18 级则灰块（minZoom/maxBounds 已钳制，正常操作到不了） | 更换演示田位置时 | 重跑 tools/download_tiles.py --lat/--lng（高德源，含 GCJ-02 换算） |
| 高德 GCJ-02 坐标偏移 | WGS84 轨迹叠加高德底图有百米级固有偏移 | 演示被追问精度、或做空间分析时 | 前端轨迹坐标统一转 GCJ-02（换算函数在 download_tiles.py 可复用） |
| map-check.html 验证页公开可达 | 调试页面留在 public/，对外可见 | 部署生产/公网前 | 删除或加访问控制 |

## 已清

| 债 | 清偿 | commit |
|---|---|---|
| 地图 403 泥潭（三代方案迭代） | 终案=高德本地瓦片：OSM 方案死于反滥用封锁+缓存毒化、CARTO 死于无 key 占位图，教训与时间线见本表下方复盘 | 0d8d1d6→f9dea14 |
| 地图离线瓦片（初版） | 被「地图 403 泥潭」条目取代 | 615c4ce |
| D 系列决策定义佚失 | git 考古补回 + decision-registry.md 建立 | e004cb9 |
| 前端 AI 味重 | 全站改专业 SaaS 亮色风 | f1cfc03 |
| echarts 全量引入 1.28MB | 按需注册降至 740KB | f3ee850 |

## 复盘：地图 403 事件时间线（2026-08-27~28，五代方案）

| 版本 | 方案 | 死因 |
|---|---|---|
| MV1-4 | OSM 本地缓存 + 浏览器端在线回退 | ① 批量下载触发 OSM 反滥用封锁（IP 级，连坐浏览器）；② 回退机制写错（onTileError 是 OpenLayers 接口，Leaflet 不调用）；③ CARTO 改政策无 key 返回错误占位图 |
| MV5-6 | 砍在线回退 + 换路径 /maptiles | 方向正确但两个盲区：缓存半径 1.2km 小于视口（边缘 404 灰块）；旧 URL 的浏览器缓存毒条目仍在 |
| MV7 | minZoom 13 + 日志陷阱 | 服务端日志全量记录 107 个 404，定位「缩小地图越出缓存下限」——铁证方法论确立 |
| **MV8** | **高德本地瓦片（终案）** | 新 URL 空间 + 新上游 + GCJ-02 换算，前三代的失败因子全部规避 |

**方法论教训**（已体现于 core-beliefs.md §7）：
1. 「显示异常」先上服务端日志抓真实请求，不靠用户截图猜——截图猜错两轮，日志一上立破案
2. 浏览器磁盘缓存按 URL 键存取：URL 不变，服务端怎么修都无效；换路径是最便宜的解毒剂
3. 第三方免费瓦片源都不可作为演示依赖（封锁/政策/key 三种死法），唯一可靠源是自己预缓存的本地瓦片
4. 验证页（map-check.html）+ 版本徽标（MV{n}）是排查浏览器侧问题的利器：隔离变量 + 一眼识别实际运行代码

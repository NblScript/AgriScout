# 0001 · 软件线 M0–M6（历史回填）

> 2026-08-27 归档回填：M0–M6 开发先于执行计划制度，此处从 git log 提炼存档。
> 每个 commit 即一个里程碑，细节看对应 commit message 与主计划 §10。

## 状态
已完成 ✅（commit `330f605` → `bd473b3`，另有 f3ee850 M6+ 增量）。

## 里程碑台账

| 里程碑 | commit | 交付物 |
|---|---|---|
| M0 工程骨架 | `330f605` | FastAPI 后端 + Vue3 前端 + 设计文档 |
| M1 基础管理 | `8b54152` | 四表 CRUD（Alembic 迁移）+ Element Plus 管理页 + 开发基线定稿（B1–B4） |
| M2 数据接入 | `0a83f62` | 巡检包协议 v1 + 单事务落库 + 照片内容寻址存储 + 分页/bbox 查询 |
| M3 分析管线 | `ee3e0ae` | Analyzer 协议 + 占位颜色识别 + 后台逐点分析（D2）+ 结果查询 |
| M4 建议引擎 | `9db727c` | 三层规则匹配 + YAML 种子库 18 条 + rule_snapshot 快照冻结 + 采纳/驳回决策保护 |
| M5 模拟器 | `7accae4` | 独立虚拟巡田（D5：只走 HTTP），三场景闭环实测 |
| M6 可视化 | `bd473b3` | Leaflet 地图回放 + 时间轴 + 可溯源建议面板，软件线收官 |
| M6+ 回流+总览 | `f3ee850` | Annotation 表 + 复核区块 + NDJSON 训练集导出；/screen 数据总览 + 回放页亮色化（f1cfc03） |

## 最终结果

软件线 M0–M6 全部完成，38 项后端测试全绿；识别线停留在 L0 占位，
接续计划见 `0002-m-ai-yolo-pipeline.md`。
